#include "main.h"
#include <stdio.h>

/**
 * print_times_table - print n time table
 * Return: 0
 */

void print_times_table(int n)
{
	int i;
	int j;
	int result;

	while (n >= 0 || n <= 15)
	{
		for (i = 0 ; i <= 15 ; i++)
	{
		for (j = 0 ; j <= 15 ; j++)
		
		result = i * j;
		{
		if (j == 0)
			printf("%d, ", result);
	}
		printf("\n");
		return (void);
}
}
}


